import styled from "styled-components";

const HomeMain = styled.main`
  margin-top: 4rem;
  padding: 1rem;
  text-align: center;
`;

const HomeHeading = styled.h2`
  font-size: 1.8rem;
`;

const StrongP = styled.p`
  font-weight: bold;
  font-size: 1.5rem;
  margin: 2rem auto;
`;

const DescriptionP = styled.p`
  max-width: 50rem;
  margin: auto;
  font-size: 1.2rem;
`;

export { HomeMain, HomeHeading, StrongP, DescriptionP };
